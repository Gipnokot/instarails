<h2 class="text-3xl font-bold mb-8 text-center text-primary">Resend Unlock Instructions</h2>

<%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "max-w-md mx-auto bg-white p-8 rounded-2xl shadow-lg space-y-6" }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div>
    <%= f.label :email, class: "block text-sm font-semibold mb-1" %>
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "input input-bordered w-full" %>
  </div>

  <div class="text-center">
    <%= f.submit "Resend unlock instructions", class: "btn btn-primary btn-wide" %>
  </div>
<% end %>

<div class="mt-6 text-center">
  <%= render "devise/shared/links" %>
</div>
