<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Instarails" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- PWA -->
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Styles -->
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- JS -->
    <%= javascript_importmap_tags "application" %>
  </head>

  <body class="bg-base-100 text-base-content flex flex-col min-h-screen">
    <!-- Header -->
    <%= render "shared/header" %>

    <!-- Flash messages -->
    <div class="container mx-auto px-4 mt-4">
      <% if notice %>
        <div class="alert alert-success shadow-lg mb-4 flex items-center gap-2">
          <i class="lucide lucide-check-circle"></i>
          <span><%= notice %></span>
        </div>
      <% end %>
      <% if alert %>
        <div class="alert alert-error shadow-lg mb-4 flex items-center gap-2">
          <i class="lucide lucide-alert-circle"></i>
          <span><%= alert %></span>
        </div>
      <% end %>
    </div>

    <!-- Main content -->
    <main class="container mx-auto px-4 flex-grow">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="footer footer-center p-10 bg-base-200 text-base-content mt-auto">
      <aside>
        <i class="lucide lucide-heart text-red-500 text-3xl mb-2"></i>
        <p class="font-bold text-lg">InstaRails</p>
        <p class="text-sm">&copy; <%= Time.current.year %> — Сделано на Ruby on Rails.</p>
      </aside>
      <nav>
        <div class="grid grid-flow-col gap-4">
          <%= link_to 'Главная', root_path, class: "link link-hover" %>
          <%= link_to 'О проекте', '#', class: "link link-hover" %>
          <%= link_to 'GitHub', 'https://github.com/Gipnokot/instarails/', class: "link link-hover", target: "_blank" %>
        </div>
      </nav>
    </footer>
  </body>
</html>
